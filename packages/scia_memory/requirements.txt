# SCIA Memory — Dependencies
# Core (required)
# None — core memory uses only Python standard library

# Optional — Redis persistence
redis>=5.0

# Optional — Frame persistence REST API
aiohttp>=3.9
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
