# SRT-1 Architecture Recovery Inventory

**Date:** 2026-06-23
**Branch:** recovery/srt1-architecture-cleanup
**Version:** 2.3.3
**Total files:** 98 source files (excluding .git)

---

## 1. Working Modules

| Module | Location | Status | Notes |
|--------|----------|--------|-------|
| **Code Indexer** | `srt1_code_indexer/indexer.py` | Working | AST parser, symbol table, manifest generation |
| **SRT Engine** | `srt1_code_indexer/srt.py` | Working | Seed/reflection/enforcement core |
| **Unified Engine** | `srt1_code_indexer/engine.py` | Working | 3110-line monolith: index + inject + serve + watch |
| **Authority Client** | `srt1_code_indexer/authority_client.py` | Working | Signing service integration |
| **MCP Server** | `srt1_platform/mcp_server.py` | Working | Model Context Protocol for real-time alignment |
| **Middleware** | `srt1_platform/middleware.py` | Working | Live HTTP middleware with reflection |
| **Seed Queue** | `srt1_platform/seed_queue.py` | Working | Seed lifecycle: plant/germinate/grow/bloom/wilt |
| **Execution Bridge** | `srt1_platform/execution_bridge.py` | Working | Dispatches seeds to assistants |
| **Seed Templates** | `srt1_pro/seed_templates.py` | Working | Template registry with 10+ built-in templates |
| **Context Bundler** | `srt1_pro/context_bundler.py` | Working | Phase 2: task-aware context assembly |
| **Execution Engine** | `srt1_pro/execution_engine.py` | Working | Phase 3: dispatch to assistant |
| **Self Heal** | `srt1_pro/self_heal.py` | Working | Governed remediation engine |
| **Completeness** | `srt1_pro/completeness.py` | Working | Seed-to-tree validator |
| **Analytics** | `srt1_pro/analytics.py` | Working | Dashboard metrics engine |
| **Reinjector** | `srt1_pro/reinjector.py` | Working | Live reinjection layer |
| **Workspace Connector** | `srt1_pro/workspace_connector.py` | Working | Cross-module connector (constellation basis) |
| **Auto Injector** | `srt1_platform/auto_injector.py` | Working | Context file generator (CLAUDE.md etc.) |
| **Delta Auditor** | `srt1_platform/delta_auditor.py` | Working | Architecture drift detection |
| **Thread Recovery** | `srt1_platform/thread_recovery.py` | Working | Seed miner from conversation exports |
| **Tracing System** | `srt1_platform/tracing_system.py` | Working | Universal tracing, execution graphs, validation |
| **Remote Auth** | `srt1_platform/remote_auth.py` | Working | Token-based auth for remote access |
| **Proxy Engine** | `srt1_platform/proxy_engine.py` | Working | LLM proxy attribution engine |
| **Memory System** | `packages/scia_memory/` | Working | 7 modules: reflex, regenerative, redis, orchestrator |
| **Security Package** | `packages/scia_security/` | Working | 6 modules: audit, encryption, execution graph, integrity, signing |
| **Seed Extract API** | `developer-pwa/api/extract_seeds.py` | Working | Conversation seed extraction |
| **Parse Export API** | `developer-pwa/api/parse_export.py` | Working | ChatGPT/Claude export parser |

---

## 2. Duplicate Modules & Files

### Code Duplicates (flagged by SRT-1)

| Function | Location 1 | Location 2 | Resolution |
|----------|-----------|-----------|------------|
| `_get_conn()` | `scia_security/audit_log.py:55` | `scia_security/execution_graph.py:86` | Consolidate to `db_utils.py` (already exists) |
| `_close_conn()` | `scia_security/audit_log.py:62` | `scia_security/execution_graph.py:93` | Consolidate to `db_utils.py` |
| `_init_db()` | `scia_security/audit_log.py:66` | `scia_security/execution_graph.py:97` | Consolidate to `db_utils.py` |

**Note:** `packages/scia_security/scia_security/db_utils.py` already contains `SQLiteDatabaseManager` base class that solves this. The two files just don't use it yet.

### Dashboard/PWA Duplicates

| Asset | Location 1 (developer-pwa/) | Location 2 (srt1_platform/pwa/) | Status |
|-------|---------------------------|-------------------------------|--------|
| `dashboard.html` | 236 KB, 2867 lines | 235 KB, 2848 lines | Near-duplicate, different hashes |
| `mobile.html` | 69 KB | 69 KB | Near-duplicate |
| `style.css` | 12.8 KB | 12.8 KB | **Identical** (same hash) |
| `platform.js` | `js/platform.js` | `js/platform.js` | Near-duplicate |
| `pwa-install.js` | root | root | Near-duplicate |
| `sw.js` | root | root | Near-duplicate |
| `manifest.json` | root | root | Near-duplicate |

**Additional:** `srt1_platform/mobile/index.html` (30 KB) is a third mobile variant.
**Additional:** `developer-pwa/api/platform.js` is a third copy of platform.js.

### Generated Context Files (Identical)

| File | Size | Notes |
|------|------|-------|
| `CLAUDE.md` | 94 KB | Generated by SRT-1 auto_injector |
| `.cursorrules` | 94 KB | **Identical** to CLAUDE.md (same hash) |
| `AGENTS.md` | 121 KB | Generated, different format |

---

## 3. Generated Files

| File | Generator | Regenerable? |
|------|-----------|-------------|
| `CLAUDE.md` | `auto_injector.py` / `engine.py` | Yes |
| `.cursorrules` | `auto_injector.py` / `engine.py` | Yes |
| `AGENTS.md` | `auto_injector.py` / `engine.py` | Yes |

These are regenerated on every `srt1` run. They should be in `.gitignore` but are currently tracked.

---

## 4. Runtime Files

| Pattern | Location | In .gitignore? |
|---------|----------|---------------|
| `.srt1/` | Runtime state directory | Yes |
| `*_code_manifest.json` | Generated manifests | Yes |
| `*_context_bundle.json` | Generated bundles | Yes |
| `*_execution_result.json` | Generated results | Yes |
| `*.db` | SQLite databases | Yes |

No runtime files are committed. `.gitignore` is correctly configured.

---

## 5. Dashboard/PWA Locations

### Primary (developer-facing, feature-complete)
```
developer-pwa/
  index.html          - Landing page / marketing
  dashboard.html      - Main developer dashboard (236 KB)
  mobile.html         - Mobile PWA view (69 KB)
  auth.html           - Authentication page
  comparison.html     - Product comparison
  contact.html        - Contact page
  documentation.html  - Documentation
  workspace-demo.html - Workspace demo
  manifest.json       - PWA manifest
  sw.js               - Service worker
  pwa-install.js      - PWA install helper
  api/
    extract_seeds.py  - Seed extraction API
    parse_export.py   - Export parser API
    platform.js       - API platform layer
  js/
    platform.js       - Frontend platform layer
  assets/
    style.css         - Stylesheet
  templates/          - 14 HTML partials (extracted components)
```

### Secondary (PyPI package bundle)
```
srt1_platform/pwa/
  __init__.py
  dashboard.html      - Dashboard copy (235 KB)
  mobile.html         - Mobile copy (69 KB)
  manifest.json       - PWA manifest copy
  sw.js               - Service worker copy
  pwa-install.js      - PWA install copy
  js/platform.js      - Platform JS copy
  assets/style.css    - Stylesheet copy (identical)
```

### Tertiary (legacy mobile)
```
srt1_platform/mobile/
  index.html          - Standalone mobile view (30 KB)
```

**Canonical source:** `developer-pwa/` (more complete, has templates, has API layer)
**Package bundle:** `srt1_platform/pwa/` (subset for PyPI distribution via `package-data` in pyproject.toml)

---

## 6. Broken / Problematic Modules

| Issue | File | Problem |
|-------|------|---------|
| Gitignore conflict | `srt1_platform/remote_auth.py` | Listed in .gitignore but already tracked |
| Gitignore conflict | `srt1_platform/proxy_engine.py` | Listed in .gitignore but already tracked |
| Gitignore conflict | `srt1_platform/delta_auditor.py` | Listed in .gitignore but already tracked |
| Gitignore conflict | `srt1_platform/srt1_sion_cli.py` | Listed in .gitignore but already tracked |
| License gate broken | `srt1_platform/__init__.py` | References `scripts/validate_license.py` which doesn't exist |
| License gate broken | `srt1_pro/__init__.py` | References `scripts/validate_license.py` which doesn't exist |
| Missing SION | `srt1_platform/srt1_sion_cli.py` | CLI stub for SION node (not built) |
| db_utils unused | `scia_security/db_utils.py` | Created to fix duplicates but not yet wired in |

---

## 7. Stale Docs

| File | Status | Action |
|------|--------|--------|
| `CTO_HANDOVER_TO_CLAUDE.md` | Historical | Move to `docs/history/` |
| `BUILD.md` | May be stale | Review and update or archive |
| `PRODUCT_ARCHITECTURE.md` | Historical reference | Keep as architectural record |
| `SRT1_ENFORCEMENT_MODE.md` | Active spec | Keep |
| `START_SRT1.bat` | Windows convenience | Keep |

---

## 8. Files to Keep (Do Not Touch)

| File/Directory | Reason |
|----------------|--------|
| `srt1_code_indexer/` (all) | Core indexer, SRT engine, authority client |
| `srt1_pro/` (all) | Context bundler, execution, self-heal, templates, analytics |
| `srt1_platform/middleware.py` | Live middleware |
| `srt1_platform/mcp_server.py` | MCP server |
| `srt1_platform/seed_queue.py` | Seed lifecycle |
| `srt1_platform/execution_bridge.py` | Dispatch bridge |
| `srt1_platform/tracing_system.py` | Universal tracing |
| `srt1_platform/auto_injector.py` | Context file generator |
| `srt1_platform/thread_recovery.py` | Seed miner |
| `srt1_platform/remote_auth.py` | Auth system |
| `srt1_platform/proxy_engine.py` | Proxy attribution |
| `srt1_platform/delta_auditor.py` | Drift auditor |
| `packages/scia_memory/` (all) | Memory subsystem |
| `packages/scia_security/` (all) | Security subsystem |
| `developer-pwa/` (all) | Canonical PWA/dashboard |
| `pyproject.toml` | Package definition |
| `README.md` | Public-facing docs |
| `LICENSE` | Legal |
| `.gitignore` | Source control |

---

## 9. Files to Archive

| File | Destination | Reason |
|------|-------------|--------|
| `CTO_HANDOVER_TO_CLAUDE.md` | `docs/history/` | Historical handover doc |
| `srt1_platform/pwa/dashboard.html` | Archive or delete | Duplicate of developer-pwa/ |
| `srt1_platform/pwa/mobile.html` | Archive or delete | Duplicate of developer-pwa/ |
| `srt1_platform/pwa/sw.js` | Archive or delete | Duplicate |
| `srt1_platform/pwa/pwa-install.js` | Archive or delete | Duplicate |
| `srt1_platform/pwa/js/platform.js` | Archive or delete | Duplicate |
| `srt1_platform/pwa/assets/style.css` | Archive or delete | Identical duplicate |
| `srt1_platform/pwa/manifest.json` | Archive or delete | Duplicate |
| `srt1_platform/mobile/index.html` | Archive or delete | Third mobile variant |

**Decision needed:** The `srt1_platform/pwa/` directory exists to bundle PWA assets into the PyPI package (see `pyproject.toml [tool.setuptools.package-data]`). Options:
1. Keep it but make it a symlink/build-step copy from `developer-pwa/`
2. Change pyproject.toml to reference `developer-pwa/` directly
3. Keep the copy but document it as a build artifact

---

## 10. Files Not to Touch Yet

| File/Area | Reason |
|-----------|--------|
| `developer-pwa/templates/` | 14 extracted HTML partials, recently created, untested |
| `srt1_platform/srt1_sion_cli.py` | SION node stub, future work |
| Payment/Lemon Squeezy refs in HTML | Business logic, out of scope for architecture cleanup |
| `packages/scia_memory/orchestrator_api.py` | FastAPI server, requires separate testing |

---

## 11. Proposed New Folder Map

```
SRT1-CORE/
  srt1_core/                    # Renamed from srt1_code_indexer/ (or keep as-is)
    indexer.py                  # AST parser, manifest generation
    srt.py                      # Seed Reflection Tool
    authority_client.py         # Signing authority
    engine.py                   # Unified engine (future: split)

  srt1_induction/               # NEW: extracted from srt1_pro/ + srt1_platform/
    seed_templates.py           # From srt1_pro/
    context_bundler.py          # From srt1_pro/
    reinjector.py               # From srt1_pro/
    auto_injector.py            # From srt1_platform/
    execution_bridge.py         # From srt1_platform/

  srt1_sandbox/                 # NEW: logical isolation layer
    workcell.py                 # NEW: workcell registry + isolation
    sandbox.py                  # NEW: scope/permission/firewall

  srt1_constellation/           # NEW: multi-project orchestration
    workspace_connector.py      # From srt1_pro/
    constellation.py            # NEW: multi-engine registry

  srt1_stitching/               # NEW: merge-back engine
    stitcher.py                 # NEW: diff verify + merge + rollback

  srt1_interface/               # Rename of developer-pwa/
    dashboard.html
    mobile.html
    api/
    js/
    assets/
    templates/

  srt1_enterprise/              # Extracted from srt1_platform/
    remote_auth.py              # From srt1_platform/
    proxy_engine.py             # From srt1_platform/

  srt1_platform/                # Retained for shared infrastructure
    middleware.py
    mcp_server.py
    seed_queue.py
    tracing_system.py
    delta_auditor.py
    thread_recovery.py

  packages/
    scia_memory/                # Keep as-is
    scia_security/              # Keep as-is

  docs/
    recovery/                   # This inventory
    history/                    # Archived historical docs
```

**Important:** This is the TARGET layout. We will NOT move everything at once. Phase 1 focuses on cleanup and documentation. Phase 2 moves files with compatibility shims.

---

## 12. Keep / Archive / Delete List

### KEEP (no changes)
- All Python source in `srt1_code_indexer/`
- All Python source in `srt1_pro/`
- All Python source in `srt1_platform/` (except pwa/ subdirs)
- All Python source in `packages/`
- All files in `developer-pwa/`
- `pyproject.toml`, `README.md`, `LICENSE`, `MANIFEST.in`, `.gitignore`
- `PRODUCT_ARCHITECTURE.md`, `SRT1_ENFORCEMENT_MODE.md`
- `START_SRT1.bat`

### ARCHIVE (move to docs/history/)
- `CTO_HANDOVER_TO_CLAUDE.md`

### DELETE (confirmed duplicates/generated)
- `srt1_platform/pwa/dashboard.html` (duplicate of developer-pwa/)
- `srt1_platform/pwa/mobile.html` (duplicate)
- `srt1_platform/pwa/assets/style.css` (identical duplicate)
- `srt1_platform/pwa/js/platform.js` (duplicate)
- `srt1_platform/pwa/pwa-install.js` (duplicate)
- `srt1_platform/pwa/sw.js` (duplicate)
- `srt1_platform/pwa/manifest.json` (duplicate)
- `srt1_platform/mobile/index.html` (third mobile variant, superseded)

**Note:** Deleting `srt1_platform/pwa/` contents requires updating `pyproject.toml` package-data to point to `developer-pwa/` instead. The `__init__.py` stays.

### DO NOT DELETE (needs review)
- `CLAUDE.md`, `.cursorrules`, `AGENTS.md` (generated but currently serving AI tools)
- `BUILD.md` (review first)
- `developer-pwa/api/platform.js` (may be server-side variant vs client-side)

---

## 13. Migration Order

1. **Phase 0: Safety** (this commit)
   - Create recovery branch
   - Write this inventory
   - No code changes

2. **Phase 1: Clean Bloat**
   - Archive `CTO_HANDOVER_TO_CLAUDE.md` to `docs/history/`
   - Remove duplicate PWA files from `srt1_platform/pwa/` and `srt1_platform/mobile/`
   - Update `pyproject.toml` package-data to reference `developer-pwa/`
   - Wire `db_utils.SQLiteDatabaseManager` into `audit_log.py` and `execution_graph.py`
   - Fix .gitignore conflicts (remove lines for already-tracked files, or untrack them)

3. **Phase 2: Seed Lifecycle Consolidation**
   - Extend `SeedStage` enum in `seed_queue.py` with full lifecycle states
   - Add missing states: `accepted`, `blueprint_created`, `awaiting_approval`, `approved`, `verified`, `stitched`, `closed`, `terminated`

4. **Phase 3: Workcell/Sandbox Skeleton**
   - Create `srt1_sandbox/workcell.py` with Workcell dataclass
   - Create `srt1_sandbox/sandbox.py` with scope/permission model
   - No execution yet, just data structures

5. **Phase 4: Stitching Skeleton**
   - Create `srt1_stitching/stitcher.py` with minimal interface
   - Proposed diff, conflict check, verification, rollback stubs

6. **Phase 5: Package Reorganization**
   - Move files to target layout with compatibility shims
   - Update imports
   - Update pyproject.toml entry points

7. **Phase 6: Dashboard Alignment**
   - Map dashboard screens to architecture
   - No visual redesign, just data alignment

---

## 14. Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| Moving files breaks imports | High | Use compatibility shims (`from new_location import *` in old location) |
| PyPI package breaks | High | Test `pip install -e .` after each phase |
| PWA consolidation breaks served dashboard | Medium | Test `engine.py` dashboard serving after PWA changes |
| db_utils refactor breaks audit_log/execution_graph | Medium | Keep old methods as fallbacks, test individually |
| Generated files (CLAUDE.md) become stale | Low | They regenerate on next `srt1` run |
| License gate references break | Low | Already broken (scripts/ doesn't exist), fix or remove |

---

## 15. First Safe Commit Plan

**Commit 1: Recovery inventory (this commit)**
- Add `docs/recovery/SRT1_RECOVERY_INVENTORY.md`
- No code changes
- Message: "Add architecture recovery inventory"

**Commit 2: Archive historical docs**
- Move `CTO_HANDOVER_TO_CLAUDE.md` to `docs/history/`
- Message: "Archive historical CTO handover doc"

**Commit 3: Remove duplicate PWA assets**
- Delete `srt1_platform/pwa/` HTML/JS/CSS duplicates (keep `__init__.py`)
- Delete `srt1_platform/mobile/index.html`
- Update `pyproject.toml` package-data
- Message: "Remove duplicate PWA assets, consolidate to developer-pwa/"

**Commit 4: Fix db_utils duplication**
- Refactor `audit_log.py` and `execution_graph.py` to use `db_utils.SQLiteDatabaseManager`
- Message: "Consolidate duplicate _get_conn/_close_conn/_init_db to db_utils"

**Commit 5: Extend seed lifecycle**
- Add full state machine to `seed_queue.py`
- Message: "Extend seed lifecycle with approval/verification/stitching states"

---

*Recovery inventory complete. Ready for Phase 1 execution upon approval.*
